## [00:00:00]

This is Gemma 312B, a production size This is Gemma 312B, a production size large language model. 20 turns of large language model. 20 turns of large language model. 20 turns of conversation, two inference modes, and I conversation, two inference modes, and I conversation, two inference modes, and I want you to watch two things. The memory want you to watch two things. The memory want you to watch two things. The memory and the wall clock time. Standard and the wall clock time. Standard and the wall clock time. Standard inference, the KV cache. This is where inference, the KV cache. This is where inference, the KV cache. This is where the model stores its attention state and the model stores its attention state and the model stores its attention state and it grows with every turn. 48 megabytes, it grows with every turn. 48 megabytes, it grows with every turn. 48 megabytes, 195, 393, 195, 393, 195, 393, 540, 780, and 978 megabytes. Nearly a 540, 780, and 978 megabytes. Nearly a 540, 780, and 978 megabytes. Nearly a gigabyte for one conversation. And gigabyte for one conversation. And gigabyte for one conversation. And what's the wall clock time per turn? 3 what's the wall clock time per turn? 3 what's the wall clock time per turn? 3 seconds, 10 seconds, 11 seconds, 12, 13. seconds, 10 seconds, 11 seconds, 12, 13. seconds, 10 seconds, 11 seconds, 12, 13. It's getting slower every turn. The It's getting slower every turn. The It's getting slower every turn. The growing cash is dragging the model down growing cash is dragging the model down growing cash is dragging the model down the longer you talk to it. Now let's now the longer you talk to it. Now let's now the longer you talk to it. Now let's now let's look at a thing called KV direct. let's look at a thing called KV direct. let's look at a thing called KV direct. It's the same model, same conversation, It's the same model, same conversation, It's the same model, same conversation, but I've given it 150 megabyte budget. but I've given it 150 megabyte budget. but I've given it 150 megabyte budget. It will never go beyond that. The memory It will never go beyond that. The memory It will never go beyond that. The memory grows for three turns and then it's

## [00:01:00]

grows for three turns and then it's grows for three turns and then it's holding flat. Wall clock time 3.6 holding flat. Wall clock time 3.6 holding flat. Wall clock time 3.6 seconds, 3.7, seconds, 3.7, seconds, 3.7, 4.2, 4.2, 4.2, 3.9. It's stable turn after turn. Now 3.9. It's stable turn after turn. Now 3.9. It's stable turn after turn. Now look what happens at turn 10. The KV look what happens at turn 10. The KV look what happens at turn 10. The KV cache version 10.9 seconds per turn. KV cache version 10.9 seconds per turn. KV cache version 10.9 seconds per turn. KV direct direct direct 4.2 seconds. Turn 15, KV unbounded takes 4.2 seconds. Turn 15, KV unbounded takes 4.2 seconds. Turn 15, KV unbounded takes 10.9 seconds. KV direct 3.8 seconds. And 10.9 seconds. KV direct 3.8 seconds. And 10.9 seconds. KV direct 3.8 seconds. And by turn 20, KV takes 13.4 seconds. KV by turn 20, KV takes 13.4 seconds. KV by turn 20, KV takes 13.4 seconds. KV direct still at 3.9 seconds. The version direct still at 3.9 seconds. The version direct still at 3.9 seconds. The version using a sixth of the memory is running using a sixth of the memory is running using a sixth of the memory is running three times faster by wall clock. The KV three times faster by wall clock. The KV three times faster by wall clock. The KV cache, the optimization that was cache, the optimization that was cache, the optimization that was supposed to make inference fast, is now supposed to make inference fast, is now supposed to make inference fast, is now the bottleneck. The gigabyte of state it the bottleneck. The gigabyte of state it the bottleneck. The gigabyte of state it stored to save time is costing time. The stored to save time is costing time. The stored to save time is costing time. The bounded version finishes each turn while

## [00:02:00]

bounded version finishes each turn while bounded version finishes each turn while the unbounded version is still churning the unbounded version is still churning the unbounded version is still churning through its own cache. Now, think about through its own cache. Now, think about through its own cache. Now, think about what that 978 megabytes actually is. what that 978 megabytes actually is. what that 978 megabytes actually is. It's the KV cache. Key and value tends It's the KV cache. Key and value tends It's the KV cache. Key and value tends to store every layer for every token in to store every layer for every token in to store every layer for every token in the conversation. Nearly a gigabyte. And the conversation. Nearly a gigabyte. And the conversation. Nearly a gigabyte. And that grows as the context length gets that grows as the context length gets that grows as the context length gets bigger. But the thing that the KV cache bigger. But the thing that the KV cache bigger. But the thing that the KV cache is derived from the residual stream, the is derived from the residual stream, the is derived from the residual stream, the model's actual computational state is model's actual computational state is model's actual computational state is only about 8 kilobytes per token, one only about 8 kilobytes per token, one only about 8 kilobytes per token, one vector. And from that vector, all the KV vector. And from that vector, all the KV vector. And from that vector, all the KV values at that position can be recmputed values at that position can be recmputed values at that position can be recmputed with a single matrix multiply. 978 with a single matrix multiply. 978 with a single matrix multiply. 978 megabytes of KV cache derived from a megabytes of KV cache derived from a megabytes of KV cache derived from a residual stream that's a fraction of the residual stream that's a fraction of the residual stream that's a fraction of the size. The model's complete state, size. The model's complete state, size. The model's complete state, everything it needs to continue the everything it needs to continue the everything it needs to continue the conversation is in the residual stream. conversation is in the residual stream. conversation is in the residual stream. That gigabyte of cache tensors, the KV That gigabyte of cache tensors, the KV That gigabyte of cache tensors, the KV cache is a precomputed expansion of it cache is a precomputed expansion of it cache is a precomputed expansion of it and nothing nothing is lost when you

## [00:03:00]

and nothing nothing is lost when you and nothing nothing is lost when you work from the residual. Instead, the work from the residual. Instead, the work from the residual. Instead, the residual stream carries the full residual stream carries the full residual stream carries the full conversation. every fact, every entity, conversation. every fact, every entity, conversation. every fact, every entity, every relationship from every earlier every relationship from every earlier every relationship from every earlier turn compressed into the geometry of the turn compressed into the geometry of the turn compressed into the geometry of the vectors. That's not an approximation. vectors. That's not an approximation. vectors. That's not an approximation. That's the markoff property. The current That's the markoff property. The current That's the markoff property. The current state is the complete state. So in state is the complete state. So in state is the complete state. So in mathematics, a markoff process is one mathematics, a markoff process is one mathematics, a markoff process is one where the future depends only on the where the future depends only on the where the future depends only on the present state, not on how you got there, present state, not on how you got there, present state, not on how you got there, not on the history. If you know where not on the history. If you know where not on the history. If you know where you are right now, you know everything you are right now, you know everything you are right now, you know everything you need to compute for what happens you need to compute for what happens you need to compute for what happens next. And to make that a little bit more next. And to make that a little bit more next. And to make that a little bit more real, if we take something like real, if we take something like real, if we take something like tic-tac-toe for example, um I don't need tic-tac-toe for example, um I don't need tic-tac-toe for example, um I don't need to know every single move that was made to know every single move that was made to know every single move that was made in what order for a tic-tac-toe board. in what order for a tic-tac-toe board. in what order for a tic-tac-toe board. The current game state of the board The current game state of the board The current game state of the board tells me absolutely everything that I tells me absolutely everything that I tells me absolutely everything that I need to know to be able to make the next need to know to be able to make the next need to know to be able to make the next next move. I don't need to know that next move. I don't need to know that next move. I don't need to know that player one played an X and then player player one played an X and then player player one played an X and then player two made a note. The game board tells me two made a note. The game board tells me two made a note. The game board tells me exactly the state of the game as it is exactly the state of the game as it is exactly the state of the game as it is with all information that I need. The

## [00:04:00]

with all information that I need. The with all information that I need. The next move only depends on the current next move only depends on the current next move only depends on the current state. And on Thursday night, I state. And on Thursday night, I state. And on Thursday night, I hypothesize that the residual stream of hypothesize that the residual stream of hypothesize that the residual stream of a transformer has this very property. If a transformer has this very property. If a transformer has this very property. If I look at the residual stream at that I look at the residual stream at that I look at the residual stream at that point in time, then I know everything I point in time, then I know everything I point in time, then I know everything I need to know about the model. I don't need to know about the model. I don't need to know about the model. I don't need to look at the previous turns, the need to look at the previous turns, the need to look at the previous turns, the previous tokens. Everything that I need previous tokens. Everything that I need previous tokens. Everything that I need is in the residual state at that moment. is in the residual state at that moment. is in the residual state at that moment. If you haven't heard of the residual If you haven't heard of the residual If you haven't heard of the residual stream before, it's the main data stream before, it's the main data stream before, it's the main data highway through the model. It's a vector highway through the model. It's a vector highway through the model. It's a vector for Gemma 312B. for Gemma 312B. for Gemma 312B. It's 3,840 It's 3,840 It's 3,840 numbers. That vector flows through every numbers. That vector flows through every numbers. That vector flows through every layer of the model. Each layer reads it, layer of the model. Each layer reads it, layer of the model. Each layer reads it, computes something, and then writes the computes something, and then writes the computes something, and then writes the result back from the first layer to the result back from the first layer to the result back from the first layer to the very last layer. Everything flows very last layer. Everything flows very last layer. Everything flows through this one vector. And that vector through this one vector. And that vector through this one vector. And that vector at any layer is the complete at any layer is the complete at any layer is the complete computational state. Not a partial computational state. Not a partial computational state. Not a partial summary, not an approximation, the

## [00:05:00]

summary, not an approximation, the summary, not an approximation, the complete state. Everything that that complete state. Everything that that complete state. Everything that that model has computed up to that point, model has computed up to that point, model has computed up to that point, everything that it needs to compute for everything that it needs to compute for everything that it needs to compute for what comes next is sitting in that what comes next is sitting in that what comes next is sitting in that residual stream. And if [clears throat] residual stream. And if [clears throat] residual stream. And if [clears throat] that is true, then the KV cache is just that is true, then the KV cache is just that is true, then the KV cache is just a projection of it. They're a projection of it. They're a projection of it. They're deterministic functions of the state. If deterministic functions of the state. If deterministic functions of the state. If you have the state, you can recomputee you have the state, you can recomputee you have the state, you can recomputee the functions. You don't need to store the functions. You don't need to store the functions. You don't need to store them. And that's what I'm going to them. And that's what I'm going to them. And that's what I'm going to prove. Okay, three tests, real models, prove. Okay, three tests, real models, prove. Okay, three tests, real models, running code. You can verify all of this running code. You can verify all of this running code. You can verify all of this yourself. The code is up in my GitHub yourself. The code is up in my GitHub yourself. The code is up in my GitHub repo. I'll put that in the description. repo. I'll put that in the description. repo. I'll put that in the description. So test one, I compute the KV cache the So test one, I compute the KV cache the So test one, I compute the KV cache the standard way. So in K and V during the standard way. So in K and V during the standard way. So in K and V during the forward pass. Then I recmp compute KMV forward pass. Then I recmp compute KMV forward pass. Then I recmp compute KMV purely from the residual stream. And at purely from the residual stream. And at purely from the residual stream. And at each layer, I compare them. So in this each layer, I compare them. So in this each layer, I compare them. So in this case, I'm going to run it against Gemma case, I'm going to run it against Gemma case, I'm going to run it against Gemma 3270M, 3270M, 3270M, which is a slightly smaller model than which is a slightly smaller model than which is a slightly smaller model than the 12B. You can see it's got 18 layers. the 12B. You can see it's got 18 layers. the 12B. You can see it's got 18 layers. And then we're just looking across all And then we're just looking across all And then we're just looking across all 18 layers. At this point, it's a prompt

## [00:06:00]

18 layers. At this point, it's a prompt 18 layers. At this point, it's a prompt of six tokens. And then I'm comparing of six tokens. And then I'm comparing of six tokens. And then I'm comparing the KMV between our version and the the KMV between our version and the the KMV between our version and the cache version. And you can see pass pass cache version. And you can see pass pass cache version. And you can see pass pass pass. All layers difference zero. The KV pass. All layers difference zero. The KV pass. All layers difference zero. The KV cache is exactly recoverable from the cache is exactly recoverable from the cache is exactly recoverable from the residual. So what I've done is I've residual. So what I've done is I've residual. So what I've done is I've compared the residual stream projection compared the residual stream projection compared the residual stream projection of the KV cache versus the actual stored of the KV cache versus the actual stored of the KV cache versus the actual stored KV cache and it's the same and that's KV cache and it's the same and that's KV cache and it's the same and that's every single layer exactly zero not every single layer exactly zero not every single layer exactly zero not approximately zero exactly zero not approximately zero exactly zero not approximately zero exactly zero not within floatingoint tolerance exactly within floatingoint tolerance exactly within floatingoint tolerance exactly zero the KV values at every layer are a zero the KV values at every layer are a zero the KV values at every layer are a deterministic function of the residual deterministic function of the residual deterministic function of the residual at that layer the KV cache stores at that layer the KV cache stores at that layer the KV cache stores nothing that the residual doesn't nothing that the residual doesn't nothing that the residual doesn't already contain. So test two, same already contain. So test two, same already contain. So test two, same model, two inference modes, standard model, two inference modes, standard model, two inference modes, standard with KV cache and then residual stream with KV cache and then residual stream with KV cache and then residual stream with no KV cache. We're going to with no KV cache. We're going to with no KV cache. We're going to generate it side by side. So let's take generate it side by side. So let's take generate it side by side. So let's take a look at this. Same model, the GEMA 3 a look at this. Same model, the GEMA 3 a look at this. Same model, the GEMA 3 270M instruction shouldn't be BF16 270M instruction shouldn't be BF16 270M instruction shouldn't be BF16 version. And again, we're running 50

## [00:07:00]

version. And again, we're running 50 version. And again, we're running 50 tokens. So what we're going to do for tokens. So what we're going to do for tokens. So what we're going to do for each token is we're going to compare the each token is we're going to compare the each token is we're going to compare the standard KV on each token generation to standard KV on each token generation to standard KV on each token generation to the KV calculated by the RS residual. the KV calculated by the RS residual. the KV calculated by the RS residual. Now if we look at this, you can kind of Now if we look at this, you can kind of Now if we look at this, you can kind of see the KV cache growing as we go along, see the KV cache growing as we go along, see the KV cache growing as we go along, which is fine. Um, and then if we look which is fine. Um, and then if we look which is fine. Um, and then if we look at the RS residual, this match column is at the RS residual, this match column is at the RS residual, this match column is is just basically doing a comparison. is just basically doing a comparison. is just basically doing a comparison. Every single token, all 50 tokens, boom, Every single token, all 50 tokens, boom, Every single token, all 50 tokens, boom, complete match. It has generated the KV complete match. It has generated the KV complete match. It has generated the KV exactly. exactly. exactly. Just to be clear, the standard KV cache Just to be clear, the standard KV cache Just to be clear, the standard KV cache 3.7 meg and [clears throat] it grows 3.7 meg and [clears throat] it grows 3.7 meg and [clears throat] it grows with every new token. The RS residual is with every new token. The RS residual is with every new token. The RS residual is recomputed fresh. No persistent KV recomputed fresh. No persistent KV recomputed fresh. No persistent KV state. You see this 14x difference. By state. You see this 14x difference. By state. You see this 14x difference. By the way, this gets absolutely massive the way, this gets absolutely massive the way, this gets absolutely massive when you get to the 12 billion parameter when you get to the 12 billion parameter when you get to the 12 billion parameter model. And there we go. All 50 tokens model. And there we go. All 50 tokens model. And there we go. All 50 tokens identical. The residual stream is identical. The residual stream is identical. The residual stream is producing the same output as the KB producing the same output as the KB producing the same output as the KB cache. And what does this prove? It

## [00:08:00]

cache. And what does this prove? It cache. And what does this prove? It proves it's correct. It proves it's proves it's correct. It proves it's proves it's correct. It proves it's marov. You can see the memory marov. You can see the memory marov. You can see the memory difference. This is tiny. I could run difference. This is tiny. I could run difference. This is tiny. I could run this on a Raspberry Pi if I wanted to. this on a Raspberry Pi if I wanted to. this on a Raspberry Pi if I wanted to. Now, you probably noticed that there was Now, you probably noticed that there was Now, you probably noticed that there was a speed difference on the RS version. It a speed difference on the RS version. It a speed difference on the RS version. It was running uh a lot slower because I was running uh a lot slower because I was running uh a lot slower because I was recmputing it every time. But when was recmputing it every time. But when was recmputing it every time. But when you see my KV direct version, which is you see my KV direct version, which is you see my KV direct version, which is the same version I ran at the beginning the same version I ran at the beginning the same version I ran at the beginning of the uh uh the the video, then what of the uh uh the the video, then what of the uh uh the the video, then what you will notice here is what I'm doing you will notice here is what I'm doing you will notice here is what I'm doing is that calculation of the KV cache is that calculation of the KV cache is that calculation of the KV cache precomputing it uh when I refill it from precomputing it uh when I refill it from precomputing it uh when I refill it from the RS. So let's see this one more time the RS. So let's see this one more time the RS. So let's see this one more time and look here. So same same comparison and look here. So same same comparison and look here. So same same comparison as before. We're using the same GMA 3 uh as before. We're using the same GMA 3 uh as before. We're using the same GMA 3 uh 270m uh model. And by the way, we can do 270m uh model. And by the way, we can do 270m uh model. And by the way, we can do this on uh any large model. Um and then this on uh any large model. Um and then this on uh any large model. Um and then you can see the tokens, all 50 tokens, you can see the tokens, all 50 tokens, you can see the tokens, all 50 tokens, the KV direct version completely the KV direct version completely the KV direct version completely matching across the board. Um but look matching across the board. Um but look matching across the board. Um but look at the speed difference there. The KV at the speed difference there. The KV at the speed difference there. The KV direct is running so much faster. It's

## [00:09:00]

direct is running so much faster. It's direct is running so much faster. It's still a little bit slower than running still a little bit slower than running still a little bit slower than running KV cache directly. But then once we KV cache directly. But then once we KV cache directly. But then once we start handling prefills and we do start handling prefills and we do start handling prefills and we do multi-turn etc. then what you'll notice multi-turn etc. then what you'll notice multi-turn etc. then what you'll notice is the KV uh direct actually uh is the KV uh direct actually uh is the KV uh direct actually uh outstrips the uh standard KV with the outstrips the uh standard KV with the outstrips the uh standard KV with the memory uh projections from the memory uh projections from the memory uh projections from the beginning. So standard KV 4.8 beginning. So standard KV 4.8 beginning. So standard KV 4.8 milliseconds per token. KV direct 8.7. milliseconds per token. KV direct 8.7. milliseconds per token. KV direct 8.7. Residual stream was 17. So we've sped Residual stream was 17. So we've sped Residual stream was 17. So we've sped this up from the previous version. The this up from the previous version. The this up from the previous version. The residual stream proved the theory that residual stream proved the theory that residual stream proved the theory that the KV cache is redundant. The KV direct the KV cache is redundant. The KV direct the KV cache is redundant. The KV direct gets the speed back by storing K&amp;V gets the speed back by storing K&amp;V gets the speed back by storing K&amp;V directly after computing them, skipping directly after computing them, skipping directly after computing them, skipping that residual round trip. Same that residual round trip. Same that residual round trip. Same correctness, same memory, and it's correctness, same memory, and it's correctness, same memory, and it's nearly the same speed as standard KV. nearly the same speed as standard KV. nearly the same speed as standard KV. So, if we come back to the very same So, if we come back to the very same So, if we come back to the very same demo that we ran at the beginning there, demo that we ran at the beginning there, demo that we ran at the beginning there, there's the unbounded cache. By turn 20, there's the unbounded cache. By turn 20, there's the unbounded cache. By turn 20, it was using nearly a gigabyte of it was using nearly a gigabyte of it was using nearly a gigabyte of memory. And that's only with a context memory. And that's only with a context memory. And that's only with a context of 2,000 tokens. Think of that when of 2,000 tokens. Think of that when of 2,000 tokens. Think of that when we're running thousands and thousands of we're running thousands and thousands of we're running thousands and thousands of tokens and much bigger models. This is

## [00:10:00]

tokens and much bigger models. This is tokens and much bigger models. This is just going to slow down and the wall just going to slow down and the wall just going to slow down and the wall time is going to increase. And then this time is going to increase. And then this time is going to increase. And then this version, the Bandy KV direct, same one version, the Bandy KV direct, same one version, the Bandy KV direct, same one as we showed you before. Storing that KV as we showed you before. Storing that KV as we showed you before. Storing that KV values directly after the prefill. No values directly after the prefill. No values directly after the prefill. No recomputed step time. It's the same recomputed step time. It's the same recomputed step time. It's the same memory token as standard KV. It matches memory token as standard KV. It matches memory token as standard KV. It matches standard KV speed, but look at the wall standard KV speed, but look at the wall standard KV speed, but look at the wall time. So much more consistent. And look, time. So much more consistent. And look, time. So much more consistent. And look, the memory is not growing. I've kept it the memory is not growing. I've kept it the memory is not growing. I've kept it consistently 150meg. And you see the consistently 150meg. And you see the consistently 150meg. And you see the difference here. Unbounded, you know, difference here. Unbounded, you know, difference here. Unbounded, you know, was peing at a gig, but that could go so was peing at a gig, but that could go so was peing at a gig, but that could go so much bigger. Whereas bounty KV, I've much bigger. Whereas bounty KV, I've much bigger. Whereas bounty KV, I've kept it to 150 meg. And basically, I kept it to 150 meg. And basically, I kept it to 150 meg. And basically, I could run this on tiny devices. The could run this on tiny devices. The could run this on tiny devices. The tokens per second are roughly the same, tokens per second are roughly the same, tokens per second are roughly the same, but the wall time is so much faster. So but the wall time is so much faster. So but the wall time is so much faster. So much faster. And look at the budget. 150 much faster. And look at the budget. 150 much faster. And look at the budget. 150 meg. So, let me talk a little bit about meg. So, let me talk a little bit about meg. So, let me talk a little bit about let me talk about a little bit of the let me talk about a little bit of the let me talk about a little bit of the architecture. What's going on here? The architecture. What's going on here? The architecture. What's going on here? The active window which holds the KN&amp;V active window which holds the KN&amp;V active window which holds the KN&amp;V tensors and device memory for the 12 tensors and device memory for the 12 tensors and device memory for the 12 billion parameter model. We set a budget billion parameter model. We set a budget billion parameter model. We set a budget of 150 megabytes. That basically holds

## [00:11:00]

of 150 megabytes. That basically holds of 150 megabytes. That basically holds around 400 tokens. KN&amp;V and already around 400 tokens. KN&amp;V and already around 400 tokens. KN&amp;V and already computed ready for attention to use computed ready for attention to use computed ready for attention to use immediately. When when a new turn immediately. When when a new turn immediately. When when a new turn arrives, we run a single forward pass arrives, we run a single forward pass arrives, we run a single forward pass extend which computes KMV for the new extend which computes KMV for the new extend which computes KMV for the new tokens and concatenates them to the tokens and concatenates them to the tokens and concatenates them to the storage. So it's basically working as it storage. So it's basically working as it storage. So it's basically working as it as it did before. At generation time, as it did before. At generation time, as it did before. At generation time, each new token only computes its only each new token only computes its only each new token only computes its only KMV. The old the old K andV are already KMV. The old the old K andV are already KMV. The old the old K andV are already there. No recomputee. That's why that's there. No recomputee. That's why that's there. No recomputee. That's why that's why it's fast. As a conversation grows, why it's fast. As a conversation grows, why it's fast. As a conversation grows, the window slides. The oldest tokens the window slides. The oldest tokens the window slides. The oldest tokens leave the active window. Now, you're leave the active window. Now, you're leave the active window. Now, you're probably thinking to yourself, I hear a probably thinking to yourself, I hear a probably thinking to yourself, I hear a slide in window and I'm thinking I'm slide in window and I'm thinking I'm slide in window and I'm thinking I'm losing context. And you're not. You do losing context. And you're not. You do losing context. And you're not. You do with unbounded. So, anyone who's done KV with unbounded. So, anyone who's done KV with unbounded. So, anyone who's done KV caching before, when you've applied caching before, when you've applied caching before, when you've applied binding to KV cache, you drop off the binding to KV cache, you drop off the binding to KV cache, you drop off the tokens, you lose context. With a tokens, you lose context. With a tokens, you lose context. With a residual stream, you you don't lose residual stream, you you don't lose residual stream, you you don't lose context at all. Um, and this is the key context at all. Um, and this is the key context at all. Um, and this is the key insight because the residual stream, insight because the residual stream, insight because the residual stream, remember what I said is marov. So, I

## [00:12:00]

remember what I said is marov. So, I remember what I said is marov. So, I want you to think about what's in those want you to think about what's in those want you to think about what's in those residuals. Token 400 in the window residuals. Token 400 in the window residuals. Token 400 in the window wasn't computed in isolation. When a wasn't computed in isolation. When a wasn't computed in isolation. When a model originally processed it, every model originally processed it, every model originally processed it, every layer ran attention over the full layer ran attention over the full layer ran attention over the full context. Every token that came before context. Every token that came before context. Every token that came before it, the result of that attention, the it, the result of that attention, the it, the result of that attention, the information from the entire conversation information from the entire conversation information from the entire conversation was written into that residual. That's was written into that residual. That's was written into that residual. That's what the residual stream does. It what the residual stream does. It what the residual stream does. It accumulates layer by layer, position by accumulates layer by layer, position by accumulates layer by layer, position by position, and the model compresses the position, and the model compresses the position, and the model compresses the full context into each token state full context into each token state full context into each token state vector. So the residuals in the window vector. So the residuals in the window vector. So the residuals in the window already carry the earlier tokens. Not a already carry the earlier tokens. Not a already carry the earlier tokens. Not a summary, not an approximation, the summary, not an approximation, the complete marov state. Just like the complete marov state. Just like the complete marov state. Just like the tic-tactoe board, everything the model tic-tactoe board, everything the model tic-tactoe board, everything the model attended to, every entity, every fact, attended to, every entity, every fact, attended to, every entity, every fact, every relationship from the entire every relationship from the entire every relationship from the entire conversation is baked into that geometry conversation is baked into that geometry conversation is baked into that geometry of vectors that remain in the window. of vectors that remain in the window. of vectors that remain in the window. The older tokens aren't gone. They're

## [00:13:00]

The older tokens aren't gone. They're The older tokens aren't gone. They're inside the surviving residuals. That's inside the surviving residuals. That's inside the surviving residuals. That's the markoff property in action. There's the markoff property in action. There's the markoff property in action. There's no trade-off. The window is the full no trade-off. The window is the full no trade-off. The window is the full conversation. See through the compressed conversation. See through the compressed conversation. See through the compressed lens of the residual stream bounded lens of the residual stream bounded lens of the residual stream bounded memory, complete context both at the memory, complete context both at the memory, complete context both at the same time. I mean, technically I was same time. I mean, technically I was same time. I mean, technically I was running 150 megabytes there, but I could running 150 megabytes there, but I could running 150 megabytes there, but I could bring that down into something like 10 bring that down into something like 10 bring that down into something like 10 kilobytes. The only reason I'm using the kilobytes. The only reason I'm using the kilobytes. The only reason I'm using the 150 megabytes is so that I'm not 150 megabytes is so that I'm not 150 megabytes is so that I'm not precomputing every single turn. So I'm precomputing every single turn. So I'm precomputing every single turn. So I'm just letting it grow a little bit. I'm just letting it grow a little bit. I'm just letting it grow a little bit. I'm using modern-day KV cache as basically using modern-day KV cache as basically using modern-day KV cache as basically as a hot cache. And then every turn or as a hot cache. And then every turn or as a hot cache. And then every turn or every so every few turns what I'm doing every so every few turns what I'm doing every so every few turns what I'm doing is then recalculating from that point uh is then recalculating from that point uh is then recalculating from that point uh the KV and then putting it back in. But the KV and then putting it back in. But the KV and then putting it back in. But the but I'm recalculating it from the the but I'm recalculating it from the the but I'm recalculating it from the residual stream. That's how I'm gaining residual stream. That's how I'm gaining residual stream. That's how I'm gaining performance. But the key thing is the performance. But the key thing is the performance. But the key thing is the residual stream is marov. It means I

## [00:14:00]

residual stream is marov. It means I residual stream is marov. It means I never ever lose the history and that's never ever lose the history and that's never ever lose the history and that's the difference between KV cache of the difference between KV cache of the difference between KV cache of normal. It is accumulating every single normal. It is accumulating every single normal. It is accumulating every single token from the beginning and therefore token from the beginning and therefore token from the beginning and therefore the KV cache just grows and grows and the KV cache just grows and grows and the KV cache just grows and grows and grows with a residual stream. That one grows with a residual stream. That one grows with a residual stream. That one view 10 kilobytes is the equivalent of view 10 kilobytes is the equivalent of view 10 kilobytes is the equivalent of gigabytes of cash. And this is where the gigabytes of cash. And this is where the gigabytes of cash. And this is where the residual stream shows its real power. residual stream shows its real power. residual stream shows its real power. When tokens leave the active window, we When tokens leave the active window, we When tokens leave the active window, we don't lose them. We checkpoint the don't lose them. We checkpoint the don't lose them. We checkpoint the residual. Not the KV tensors, just the residual. Not the KV tensors, just the residual. Not the KV tensors, just the residual vector itself. A few kilobytes residual vector itself. A few kilobytes residual vector itself. A few kilobytes per position. I don't need to cache per position. I don't need to cache per position. I don't need to cache gigabytes. I just cache the residual gigabytes. I just cache the residual gigabytes. I just cache the residual vector, a few KB. So why the residuals vector, a few KB. So why the residuals vector, a few KB. So why the residuals and not the token IDs? Because the and not the token IDs? Because the and not the token IDs? Because the residual has already been through every residual has already been through every residual has already been through every layer, all 34 layers of attention, all layer, all 34 layers of attention, all layer, all 34 layers of attention, all 34 layers of feed forward processing. 34 layers of feed forward processing. 34 layers of feed forward processing. The complete computation is baked into The complete computation is baked into The complete computation is baked into it. And from that residual K andV are it. And from that residual K andV are it. And from that residual K andV are one matrix multiplier away. One cheap

## [00:15:00]

one matrix multiplier away. One cheap one matrix multiplier away. One cheap linear projection per head. That's it. linear projection per head. That's it. linear projection per head. That's it. If we just store the token IDs instead, If we just store the token IDs instead, If we just store the token IDs instead, we'd need to run the full forward pass we'd need to run the full forward pass we'd need to run the full forward pass to get back to the residual. Every to get back to the residual. Every to get back to the residual. Every layer, every attention computation, the layer, every attention computation, the layer, every attention computation, the entire model, that's expensive. The entire model, that's expensive. The entire model, that's expensive. The residual checkpoint skips all of that. residual checkpoint skips all of that. residual checkpoint skips all of that. It's the state after computation, not It's the state after computation, not It's the state after computation, not the input before it. And that's what we the input before it. And that's what we the input before it. And that's what we were doing with KV direct. Think of it were doing with KV direct. Think of it were doing with KV direct. Think of it this way. The KV cache is a gigabyte of this way. The KV cache is a gigabyte of this way. The KV cache is a gigabyte of precomputed projections. The residual is precomputed projections. The residual is precomputed projections. The residual is a few kilobytes of complete state that a few kilobytes of complete state that a few kilobytes of complete state that those projections can be derived from in those projections can be derived from in those projections can be derived from in a single step. The token ids are the raw a single step. The token ids are the raw a single step. The token ids are the raw inputs that would require rerunning the inputs that would require rerunning the inputs that would require rerunning the entire model to get back to this state. entire model to get back to this state. entire model to get back to this state. The residual stream is the sweet spot. The residual stream is the sweet spot. The residual stream is the sweet spot. compact enough to store, rich enough to compact enough to store, rich enough to compact enough to store, rich enough to expand the complete markoff state in a expand the complete markoff state in a expand the complete markoff state in a few KB. We also keep the token IDs 10.2 few KB. We also keep the token IDs 10.2 few KB. We also keep the token IDs 10.2 kilobytes for the full conversation. kilobytes for the full conversation. kilobytes for the full conversation. Basically, it's a lightweight fallback. Basically, it's a lightweight fallback. Basically, it's a lightweight fallback. Also, if we just want to see what was in

## [00:16:00]

Also, if we just want to see what was in Also, if we just want to see what was in the conversation, if you ever needed to the conversation, if you ever needed to the conversation, if you ever needed to reconstruct a section of the reconstruct a section of the reconstruct a section of the conversation from scratch, the tokens conversation from scratch, the tokens conversation from scratch, the tokens are the ground truth, but the residual are the ground truth, but the residual are the ground truth, but the residual checkpoints are what the architecture checkpoints are what the architecture checkpoints are what the architecture really runs on. They're the hero. The really runs on. They're the hero. The really runs on. They're the hero. The token IDs are the safety net. The model token IDs are the safety net. The model token IDs are the safety net. The model manages its memory like it managed manages its memory like it managed manages its memory like it managed yours. You don't hold every conversation yours. You don't hold every conversation yours. You don't hold every conversation you've ever had in working memory, but you've ever had in working memory, but you've ever had in working memory, but the important parts have shaped who you the important parts have shaped who you the important parts have shaped who you are right now, and you can recall the are right now, and you can recall the are right now, and you can recall the details when you need to. That's exactly details when you need to. That's exactly details when you need to. That's exactly what the architecture does. The residual what the architecture does. The residual what the architecture does. The residual stream carries the complete state. It's stream carries the complete state. It's stream carries the complete state. It's what the model navigates with. what the model navigates with. what the model navigates with. Everything else, KV, the KV cache, the Everything else, KV, the KV cache, the Everything else, KV, the KV cache, the tokens IDs, it's derived from it. The tokens IDs, it's derived from it. The tokens IDs, it's derived from it. The residual stream isn't a memory bank. residual stream isn't a memory bank. residual stream isn't a memory bank. It's a position on a map, a It's a position on a map, a It's a position on a map, a 2560dimensional 2560dimensional 2560dimensional map, and the model doesn't care how it map, and the model doesn't care how it map, and the model doesn't care how it arrived at that position. It just knows arrived at that position. It just knows arrived at that position. It just knows where it is. The next step depends only where it is. The next step depends only where it is. The next step depends only on your current position. So, I planted on your current position. So, I planted on your current position. So, I planted 50 facts into a conversation and asked

## [00:17:00]

50 facts into a conversation and asked 50 facts into a conversation and asked the model to retrieve each one. Every the model to retrieve each one. Every the model to retrieve each one. Every fact came back. No degradation. And the fact came back. No degradation. And the fact came back. No degradation. And the model wasn't storing those facts in the model wasn't storing those facts in the model wasn't storing those facts in the residual. The residual barely changed. residual. The residual barely changed. residual. The residual barely changed. What it held was a compass bearing six What it held was a compass bearing six What it held was a compass bearing six dimensions out of 2560 dimensions out of 2560 dimensions out of 2560 pointing attention back to the right pointing attention back to the right pointing attention back to the right position in context. The facts live in position in context. The facts live in position in context. The facts live in context. The residual carries the context. The residual carries the context. The residual carries the address. Six dimensions for the address, address. Six dimensions for the address, address. Six dimensions for the address, three for infrastructure. 99.6% three for infrastructure. 99.6% three for infrastructure. 99.6% of the residual is basically headroom. of the residual is basically headroom. of the residual is basically headroom. And the attention mechanism reads those And the attention mechanism reads those And the attention mechanism reads those compass bearings. The KV cache is an compass bearings. The KV cache is an compass bearings. The KV cache is an optimization not a requirement. Residual optimization not a requirement. Residual optimization not a requirement. Residual stream is the requirement. It's what the stream is the requirement. It's what the stream is the requirement. It's what the model actually computes with and it's model actually computes with and it's model actually computes with and it's only a few kilobytes of state, not a only a few kilobytes of state, not a only a few kilobytes of state, not a gigabyte of cache tensors. And once you gigabyte of cache tensors. And once you gigabyte of cache tensors. And once you build inference around that residual, build inference around that residual, build inference around that residual, once you treat it as the hero instead of once you treat it as the hero instead of once you treat it as the hero instead of the KV cache being the hero, everything the KV cache being the hero, everything the KV cache being the hero, everything changes. How models manage memory, how changes. How models manage memory, how changes. How models manage memory, how they distribute computation, how they they distribute computation, how they they distribute computation, how they scale, how they run on hardware people scale, how they run on hardware people scale, how they run on hardware people already own. This means you can take already own. This means you can take already own. This means you can take very large models and you can start to

## [00:18:00]

very large models and you can start to very large models and you can start to run them on smaller machines. And again, run them on smaller machines. And again, run them on smaller machines. And again, don't take my word for it. All the code don't take my word for it. All the code don't take my word for it. All the code is open source. Links in the is open source. Links in the is open source. Links in the description. Go prove the Marco property description. Go prove the Marco property description. Go prove the Marco property yourself. Links in the description. yourself. Links in the description. yourself. Links in the description. Anyway, hope this was useful.